version: '3.8'

services:
  opensearch_adapter:
    build: .
    container_name: opensearch_adapter
    networks:
      - test_network
    restart: unless-stopped
    ports:
      - "8005:8000" 

networks:
  test_network:
    external: true
